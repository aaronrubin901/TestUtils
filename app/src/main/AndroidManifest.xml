<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="screenovate.com.testutils">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <service
            android:name=".WifiBtUtilService"
            android:enabled="true"
            android:exported="true"></service>

        <service
            android:name="screenovate.com.testutils.WifiBtUtilService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name="screenovate.com.testutils.LastSentSmsService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name="screenovate.com.testutils.InsertNewContact"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name="screenovate.com.testutils.SendSms"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name="screenovate.com.testutils.CreateNotification"
            android:enabled="true"
            android:exported="true" />
        <activity
            android:name="screenovate.com.testutils.TestActivity"
            android:exported="true"
            android:label="@string/title_activity_test" />
        <receiver android:name="screenovate.com.testutils.CallReceiver">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.NEW_OUTGOING_CALL" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
